name: pulse
description: Pulse — Windows diagnostics
publish_to: "none"
version: 0.3.0+10

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  win32: ^5.10.0
  ffi: ^2.1.3
  pulse_protocol:
    path: ../../shared/pulse_protocol/dart
  lucide_icons_flutter: 3.1.15
  window_manager: ^0.5.2
  flutter_acrylic: ^1.1.4
  flutter_svg: ^2.3.0
  shared_preferences: ^2.5.3
  path_provider: ^2.1.5
  archive: ^4.0.7
  pdf: ^3.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/branding/
