/** PulseMCP product version (aligned with Pulse 1.0.0 product line). */
export const MCP_SERVER_VERSION = "1.0.0";

/** MCP protocol version advertised to clients (spec family). */
export const MCP_PROTOCOL_VERSION = "2025-03-26";

/** Pulse UI / product stamp (aligned with shared constants when packaged). */
export const PULSE_APP_VERSION = "1.0.0";

/** Pulse named-pipe IPC protocol version. */
export const IPC_PROTOCOL_VERSION = 1;

export const PIPE_NAME = "\\\\.\\pipe\\PulseService";

export const CLIENT_NAME = "PulseMCP";
